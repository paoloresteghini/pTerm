node_modules/
dist/
out/
.vite/
.DS_Store
.superpowers/
# Worktrees live here so two Claude sessions can work the same repo without
# fighting over one checkout. Ignored because an unignored worktree directory
# commits an entire second copy of the tree into the repository.
.claude/worktrees/
*.log
test-results/
# Playwright's HTML reporter writes here. Added on `e2e-plan-revision` ahead of
# the plan's Task 4, which turns that reporter on: without this line Task 4's
# own "git diff on production files must be empty" gate trips on the report it
# just generated.
playwright-report/
